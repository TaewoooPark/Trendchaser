# Trendchaser 2026-06-12 update

**TL;DR - the technical README now reflects the current PRISMA loop. The brief is no longer described as a simple paragraph digest: it is a four-slot, topic-card pipeline with 67 enabled source channels, an X follow-list fan-out, hallucination verification, GraphRAG export, and a race-safe `claude/brief-stream` publish path.**

This is a documentation synchronization release. The live PRISMA routine had moved faster than Trendchaser's README: source counts, slot quotas, shortlist size, validation gates, and the published brief format were all out of date. The README now matches the current loop.

---

## What changed

| Area | Now documented |
|---|---|
| Slot quotas | Morning can send up to 6 AI + 4 general; afternoon, evening, and night can send up to 4 AI + 3 general. These are upper bounds, not padding targets. |
| Source surface | 67 enabled channels plus `x_my_following`, which currently fans out to 83 X accounts. |
| Pipeline shape | Fetch, deduplicate, enrich + shortlist, score + review, write + validate, publish + deliver. |
| Candidate flow | Top 45 candidates are enriched before the deterministic 3x shortlist. |
| Freshness rules | Slot lookbacks, stale-trending caps, floor relaxation, and source-tier gates are described together. |
| Output format | Telegram gets split topic cards; the website/archive keeps `DETAIL`, diagnostics, and `TC-SCORES`. |
| Verification | The hallucination/source loop, postcheck footer, and `origin/claude/brief-stream` publish verification are called out. |
| Exports | Obsidian notes, graph clusters, and `briefs.json` are included in the architecture. |

---

## User-visible effect

No schedule or delivery channel changes in this release. The change is that the public technical documentation now describes what the running system actually does: compact cards in Telegram, deeper context on the web, fresher source gating, and a safer two-branch publish flow.

-- 2026-06-12
