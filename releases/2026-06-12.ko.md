# Trendchaser 2026-06-12 업데이트

**요약 - 기술 README를 현재 PRISMA 루프에 맞췄습니다. 이제 브리프를 단순 문단 요약이 아니라 enabled 소스 67개, X follow-list fan-out, 환각 검증, GraphRAG export, race-safe `claude/brief-stream` publish를 포함한 네 슬롯 topic-card 파이프라인으로 설명합니다.**

이번 릴리즈는 문서 동기화입니다. 실제 PRISMA routine은 이미 바뀌었는데 Trendchaser README에는 예전 소스 수, 슬롯 구성, shortlist 크기, 검증 gate, 브리프 출력 포맷이 남아 있었습니다. README를 현재 실행 루프와 맞췄습니다.

---

## 바뀐 문서 내용

| 영역 | 현재 문서화한 내용 |
|---|---|
| 슬롯 구성 | morning은 최대 AI 6 + general 4, afternoon/evening/night는 최대 AI 4 + general 3. 이 숫자는 채우기 목표가 아니라 상한입니다. |
| 소스 표면 | enabled 채널 67개 + 현재 X 83계정으로 확장되는 `x_my_following`. |
| 파이프라인 형태 | Fetch, deduplicate, enrich + shortlist, score + review, write + validate, publish + deliver. |
| 후보 흐름 | 상위 45개 후보를 enrich한 뒤 deterministic 3배수 shortlist를 구성. |
| 신선도 규칙 | 슬롯별 lookback, stale-trending cap, floor relaxation, source-tier gate를 함께 설명. |
| 출력 포맷 | Telegram은 split topic card를 받고, 웹/archive에는 `DETAIL`, diagnostics, `TC-SCORES`가 남음. |
| 검증 | 환각/source 검증 루프, postcheck footer, `origin/claude/brief-stream` publish 검증을 명시. |
| Export | Obsidian note, graph cluster, `briefs.json`을 architecture에 포함. |

---

## 사용자 체감

이번 릴리즈에서 송출 시간이나 채널은 바뀌지 않습니다. 바뀐 것은 공개 기술문서가 실제 운영 시스템을 정확히 설명하게 된 점입니다: Telegram에는 압축 card, 웹에는 더 긴 맥락, 더 엄격한 freshness gate, 더 안전한 two-branch publish 흐름이 반영되었습니다.

-- 2026-06-12
